version: '3.9'

services:
  loyalpass-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loyalpass-portal
    env_file:
      - .env
    ports:
      - "3001:3001"
    restart: unless-stopped
